# NewChatHeaderActionsMenu performance guard

Scope: the new-chat toolbar menu and its connection to the existing CLI-update-alert preference. No CLI scanner, persistence format, backend, or suppression policy changes.

| Area               | Verdict | Evidence                                                                                                                    | Change or reason kept                                                                                                                         | Verification                                                                                                     |
| ------------------ | ------- | --------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------- |
| Background work    | keep    | `NewChatHeaderActionsMenu.tsx:36` uses `useDropdownEngine`; `:75` mounts `ActionMenuSurface` only while open and positioned | Reuse open-scoped position/outside-click/keyboard listeners and their existing cleanup; add no scan, polling loop, retry, or subprocess owner | Source inspection; existing submenu and header tests passed; real desktop idle/hidden/close measurements not run |
| Memory             | keep    | `NewChatHeaderActionsMenu.tsx:25` subscribes to one persisted boolean; `:36` retains only bounded dropdown state            | No growing collections or copied version cache; leaving the start page unmounts the menu and its portal                                       | Source inspection and existing start-page tab projection tests passed                                            |
| Scope/isolation    | keep    | The switch writes `cliUpdateAlertsEnabledAtom` using its unchanged `orgii:cliUpdateAlerts:enabled` key                      | Preserve existing app-level preference scope, default, and snooze/version-mute data; do not introduce a second setting                        | Existing alert configuration and suppression tests passed                                                        |
| Rendering/hot path | keep    | Only the menu subscribes to the preference; chat content already observes it in `useCliAgentConfiguration`                  | No session-event subscription or full-history projection added to the toolbar                                                                 | Changed-file ESLint and existing focused suites passed; no runtime performance improvement claimed               |

| Resource                                     | Start / active                                                     | Idle / hidden                                                          | Close / unmount                                                                | Evidence                                                                                                                     |
| -------------------------------------------- | ------------------------------------------------------------------ | ---------------------------------------------------------------------- | ------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- |
| Menu positioning and outside-click listeners | Shared engine installs listeners when the menu opens               | No recurring interval; positioning runs in response to viewport events | Engine removes listeners, disconnects its observer, and cancels pending frames | Existing implementation inspected; desktop measurement not run                                                               |
| Submenu keyboard listener                    | Shared surface mounts after the menu opens                         | One keyboard owner for the menu tree                                   | Surface unmount removes its capture listener                                   | Existing session menu keyboard tests passed                                                                                  |
| Alert preference                             | Existing stored boolean is read when the start-page control mounts | No new autonomous work                                                 | Jotai removes the component subscription; persisted value remains              | Existing preference/suppression boundary and hook tests passed                                                               |
| Existing CLI version checks                  | Enabling alerts uses the existing selected-CLI scan path           | Existing shared recheck behavior remains unchanged                     | Disabling alerts prevents checks and unsubscribes snooze rechecks              | Hook tests cover disabled checks/manual refresh, re-enabling, shared one-timer ownership, cancellation, and per-version mute |

Network, account/endpoint/org changes, provider ingestion/rewrites, and machine topology are unchanged and outside this UI change. The existing setting is app-level rather than account-scoped. Historical data is not removed or migrated.

Integrated stack verification covers 100 tests in 22 focused suites, including the shared dropdown and creator primitives where applicable. Full TypeScript, lint, circular-dependency checking, and test placement are rerun in the isolated branch; exact outcomes appear in CreatorRefactorStack.md. The new menu reuses existing dropdown listener ownership and persists only user-selected scalar preferences. Native menu interactions and visible/hidden CPU/RSS are not measured.

Performance verdict: **blocked** for native desktop measurements. No measured CPU/RAM improvement is claimed; Computer Use was not requested.
