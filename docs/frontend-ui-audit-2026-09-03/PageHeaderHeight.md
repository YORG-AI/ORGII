# PageHeaderHeight UI audit

| Line                                                                                | Element                                                       | Verdict          | Reason                                                                                                                                               | Suggested change |
| ----------------------------------------------------------------------------------- | ------------------------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/config/workstation/tokens.ts:237`                                              | Shared inline page-header geometry                            | keep with reason | Uses the Tailwind `h-9` spacing token so inline headers match the established 36px published, Inbox, Work Items, and pull-request header contract.   | None.            |
| `src/modules/shared/layouts/SplitListHeader.tsx:32`                                 | Surface-owned header row                                      | keep with reason | Projects reuses the same 36px shell and full-width optical insets as Inbox and GitHub lists instead of introducing another local bar implementation. | None.            |
| `src/modules/ProjectManager/Projects/components/ProjectsPageHeader/index.tsx:210`   | Projects header while host tab chrome is present or collapsed | keep with reason | The host contribution is hidden and dataset, grouping, source, search, and action controls remain together in a dedicated local 36px row.            | None.            |
| `src/modules/ProjectManager/Projects/components/ProjectsPageHeader/index.tsx:234`   | Projects header fallback without a tab host                   | keep with reason | Reuses `HEADER_CLASSES.pageHeader`, keeping the no-host fallback on the same 36px contract.                                                          | None.            |
| `src/modules/ProjectManager/WorkItems/components/WorkItemsPageHeader/index.tsx:137` | Work Items header fallback without a tab host                 | keep with reason | Reuses the same shared header token; the split presentation continues to use `SplitListHeader`, which is already 36px.                               | None.            |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.
