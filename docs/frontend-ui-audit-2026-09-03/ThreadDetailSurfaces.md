# Thread detail surfaces UI audit

| Line                                                                                        | Element                                     | Verdict          | Reason                                                                                                                                                                                                                | Suggested change |
| ------------------------------------------------------------------------------------------- | ------------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/modules/shared/components/DetailFlowHeader.tsx:5`                                      | Shared title and metadata typography        | keep with reason | The formerly PR-specific 20px title and 12px activity line are now one shared token set used by PRs, issues, Work Items, and their loading state; preserving those exact values maintains the requested PR hierarchy. | None.            |
| `src/modules/shared/components/ThreadDetailTabs.tsx:44`                                     | Conversation / Linked navigation            | keep with reason | Reuses the existing `DetailTabStrip` design-system primitive, including disabled, count-loading, header, and row behavior.                                                                                            | None.            |
| `src/modules/shared/layouts/blocks/DetailTabStrip.tsx:33`                                   | PR and thread detail tab header             | keep with reason | PRs, issues, Work Items, and Inbox details share this component, its explicit 36px height, and the same 16px leading inset as the title, body, and loading state.                                                     | None.            |
| `src/modules/shared/layouts/DetailPaneLayout.tsx:121`                                       | Inbox-style detail pane header              | keep with reason | The shared pane shell selects the detail-only 36px `PanelHeader` height, preserving the generic 40px panel-header default for unrelated list and settings surfaces.                                                   | None.            |
| `src/modules/shared/components/GitHubLinkedReferences/index.tsx:115`                        | Linked reference row                        | keep with reason | Reuses the design-system `Button` for focus, keyboard, disabled, and hover behavior; the surrounding `role="listitem"` is non-interactive structure.                                                                  | None.            |
| `src/modules/shared/components/GitHubLinkedReferences/index.tsx:232`                        | Linked loading, empty, and populated states | keep with reason | Reuses shared detail width/padding plus `Placeholder`, `TimelineLoadingSkeleton`, and `TimelineCard` instead of introducing parallel loading or card treatments.                                                      | None.            |
| `src/modules/shared/components/GitHubLinkedReferences/lazy.tsx:9`                           | Linked panel lazy boundary                  | keep with reason | `React.lazy` is the appropriate demand-loading seam, and its fallback uses the same shared width, padding, and timeline skeleton as the loaded panel.                                                                 | None.            |
| `src/config/detailPanelTokens.ts:95`                                                        | Detail header and thread spacing            | keep with reason | The 36px header, title padding, and body padding are one shared PR-style contract, so loaded and loading surfaces cannot drift site by site.                                                                          | None.            |
| `src/modules/ProjectManager/WorkItems/components/WorkItemDetail/WorkItemDetailBody.tsx:142` | Main Work Item conversation                 | keep with reason | Selects the canonical thread presentation so page, panel, and Inbox Work Items all receive the same full flow title, width, body spacing, and timeline treatment.                                                     | None.            |
| `src/modules/shared/components/GitHubDetailSkeleton/index.tsx:53`                           | Loading title and metadata block            | keep with reason | The skeleton consumes `DETAIL_FLOW_HEADER_TOKENS` and the shared padding tokens, reserving the same wrapping title/subline geometry as loaded content.                                                                | None.            |
| `src/modules/MainApp/TeamInbox/components/TeamInboxDetailLayout.tsx:94`                     | Inbox header ownership                      | keep with reason | The existing shared `DetailPaneLayout` still owns actions and shell behavior; when tabs exist, they own the leading strip and the full title moves to the canonical body flow header.                                 | None.            |

Verdict totals: **0 fix**, **11 keep with reason**, **0 abstract**.
