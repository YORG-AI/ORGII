# Split header ownership UI audit

| Line                                                                                            | Element                                | Verdict          | Reason                                                                                                                                                                                                                         | Suggested change                                                                                                    |
| ----------------------------------------------------------------------------------------------- | -------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------------------------------------------- |
| `src/modules/MainApp/TeamInbox/TeamInboxView.tsx:296`                                           | Inbox split-header gate                | fix              | Header ownership varied with tab availability even though the list/detail geometry was unchanged, producing different search placement and duplicated shell logic.                                                             | Gate the local header only on whether Inbox is split rather than whether a tab bar exists.                          |
| `src/modules/MainApp/WorkManagement/GitHubWorkItemsView.tsx:323`                                | GitHub Issues / PRs split-header gate  | fix              | Issues and PRs used the same tab-dependent ownership branch as Inbox, so a folded tab row moved identical controls out of the left list.                                                                                       | Keep both compact header rows in the left column whenever the GitHub surface is split.                              |
| `src/modules/ProjectManager/WorkItems/index.tsx:420`                                            | Project Work Items split-header gate   | fix              | The project-scoped list delegated header placement through an optional host flag even though its split state already provides the necessary ownership signal.                                                                  | Derive left-header ownership directly from active List, active surface, and non-fullscreen state.                   |
| `src/modules/ProjectManager/ProjectManagerLayout/components/ProjectWorkItemsTabContent.tsx:103` | Workspace Work Items split-header gate | fix              | The workspace list repeated the optional host/tab condition and could move the same controls between two header systems.                                                                                                       | Derive left-header ownership directly from List and non-fullscreen state.                                           |
| `src/modules/MainApp/WorkManagement/WorkManagementProjectsSurface.tsx:289`                      | Dataset control handoff                | abstract         | The parent passed both a dataset control and a separate boolean that told children when to use it, creating two inputs for one layout decision.                                                                                | Always pass the compact dataset control; let each surface's split state decide whether its local header renders.    |
| `src/modules/MainApp/WorkManagement/workManagementSplitHeaderContext.ts:4`                      | Split-header context                   | abstract         | `hasTabBar` no longer affects child surface layout after adopting split-owned headers. Keeping it would invite the removed matrix to return.                                                                                   | Remove tab availability from the child context and retain only the compact dataset control.                         |
| `src/modules/MainApp/WorkManagement/index.tsx:165`                                              | Shell header projection                | keep with reason | The shell still needs its own tab-availability signal: with a visible tab row it can remove the redundant published row, while a folded row must retain global new-tab and pane controls without duplicating surface controls. | Keep `hasTabBar` at the shell boundary only, and suppress surface content whenever the split owns its local header. |

Verdict totals: **4 fix**, **1 keep with reason**, **2 abstract**.
