# Header action consistency UI audit

| Line                                                                                                                                                  | Element                                 | Verdict          | Reason                                                                                                                                                    | Suggested change                                                                                                            |
| ----------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------- |
| `src/modules/ProjectManager/WorkItems/components/WorkItemsPageHeader/WorkItemsHeaderContent.tsx:123`                                                  | Work Items header action order          | fix              | Search and maximize previously shared one slot before refresh/create, unlike the GitHub and Inbox headers, and the boundary added a decorative separator. | Keep search in the inline control slot, render domain actions next, and reserve `endControls` for maximize at the far edge. |
| `src/modules/ProjectManager/ProjectManagerLayout/components/ProjectWorkItemsTabContent.tsx:530`; `src/modules/ProjectManager/WorkItems/index.tsx:633` | Workspace and project maximize adapters | abstract         | Both Work Items variants need the same terminal presentation action, and a shared header slot prevents their order from drifting independently.           | Route both `SplitListFullscreenButton` instances through the shared `endControls` seam.                                     |
| `src/modules/shared/layouts/InboxListDetailLayout.tsx:85`                                                                                             | List/detail resting divider             | fix              | The always-visible separator adds chrome that the requested list/detail treatment does not need.                                                          | Opt this shared surface family into a transparent resting divider while retaining resizing.                                 |
| `src/scaffold/Resize/components/ResizableSplitPanel.tsx:303`                                                                                          | Transparent resize boundary             | keep with reason | The existing `ResizeHandle` variant preserves the large drag target, separator semantics, and hover feedback without drawing a persistent line.           | None.                                                                                                                       |

Verdict totals: **2 fix**, **1 keep with reason**, **1 abstract**.
