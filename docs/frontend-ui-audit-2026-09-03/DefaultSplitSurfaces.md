# DefaultSplitSurfaces UI audit

| Line                                                                                                 | Element                                | Verdict          | Reason                                                                                                                                                                                         | Suggested change |
| ---------------------------------------------------------------------------------------------------- | -------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/modules/shared/layouts/InboxListDetailLayout.tsx:50`                                            | Split / expanded presentation switch   | keep with reason | A split owns the compact list and local rows; expanding restores the pre-existing full table or list, rather than stretching the compact split list across the pane.                           | None.            |
| `src/modules/shared/layouts/SplitViewLayout.tsx:75`                                                  | Shared split-list shell                | keep with reason | Exposes the resizable and collapsible list/detail contract its production callers actually use, with one list-panel assembly so pinned headers cannot diverge through dormant layout branches. | None.            |
| `src/modules/shared/layouts/SplitListHeader.tsx:14`                                                  | Shared left-column split header        | keep with reason | Is the one shared, token-aligned shell for compact split controls; it intentionally omits a bottom divider so the header and list read as one surface.                                         | None.            |
| `src/modules/shared/layouts/SplitListFullscreenButton.tsx:17`                                        | Split expand / restore control         | keep with reason | Uses the shared small tertiary icon button and standard toolbar tooltip with an existing translated label; its caller switches between split and the established full presentation.            | None.            |
| `src/modules/shared/components/WorkManagementRefreshButton.tsx:17`                                   | Shared refresh action                  | keep with reason | Centralizes the tertiary icon button, refresh animation, accessible name, and standard toolbar tooltip so all Work Management headers expose the same interaction treatment.                   | None.            |
| `src/components/Tooltip/index.tsx:563`                                                               | Closed tooltip portal                  | keep with reason | Keeps the shared tooltip primitive responsible for its portal lifecycle; a closed tooltip renders only its trigger, avoiding empty portals and document access without changing visible UI.    | None.            |
| `src/modules/WorkStation/AppShell/WorkstationTabHeader.tsx:51`                                       | Shell-wide tab header visibility       | keep with reason | Removes the shell row only when the active split surface explicitly owns its left-column header; non-split tabs retain the stable shared row.                                                  | None.            |
| `src/components/WindowChrome/PublishedHeaderSlotsView.tsx:9`                                         | Cross-shell hidden-header slot         | keep with reason | Gives both tab hosts one explicit signal that a split surface owns the header, instead of adding a host-specific visual exception.                                                             | None.            |
| `src/engines/ChatPanel/ChatPanelHeader.tsx:356`                                                      | Chat Panel published-header stack      | keep with reason | Removes both the published row and its reserved height while the visible Chat Panel tab bar remains, so the split surface has no empty 36px gap.                                               | None.            |
| `src/engines/ChatPanel/header/ChatPanelPublishedHeader.tsx:28`                                       | Chat Panel shared header guard         | keep with reason | Defends the row component itself against accidental rendering when the active split surface has moved its controls to the left column.                                                         | None.            |
| `src/modules/MainApp/WorkManagement/workManagementSplitHeaderContext.ts:14`                          | Work Management header handoff         | keep with reason | Carries only the compact dataset switch into split children; split geometry now determines local header ownership without a parallel tab-availability input.                                   | None.            |
| `src/modules/MainApp/WorkManagement/WorkManagementDatasetSwitch.tsx:27`                              | Compact split dataset switch           | keep with reason | Uses the existing Select and icon system, reducing only the split-header trigger to its selected dataset icon and chevron while retaining a named accessible control and readable menu rows.   | None.            |
| `src/engines/ChatPanel/ChatPanelShell.tsx:105`                                                       | Tab-row visibility propagation         | keep with reason | Passes the shell's actual folded/visible tab-row state to Work Management so every similar surface applies the same header-placement rule.                                                     | None.            |
| `src/modules/MainApp/WorkManagement/index.tsx:181`                                                   | Chat Panel header projection           | keep with reason | Relays the split-owned signal only while a tab row remains visible; when that row folds away, the published header stays visible as the pane's single top header.                              | None.            |
| `src/modules/MainApp/WorkManagement/GitHubWorkItemsView.tsx:327`                                     | GitHub two-row split header            | keep with reason | Places dataset/repository/state in the first left-column row and search/actions in the second whenever the surface is split, independent of tab-bar availability.                              | None.            |
| `src/modules/MainApp/WorkManagement/GitHubWorkItemsView.tsx:292`                                     | Full-width GitHub header grouping      | keep with reason | Keeps repository scope, open/closed state, and personal filters as one left-side context cluster, separated from the right-side search/action cluster by the shell's flexible space.           | None.            |
| `src/modules/MainApp/WorkManagement/GitHubWorkItemsHeaderControls.tsx:97`                            | GitHub search/action separation        | keep with reason | Lets the search consume available width, reserves a token `gap-1` before its right-side icon group, and retains one-pixel spacing within that group.                                           | None.            |
| `src/modules/shared/components/WorkManagementSearchInput.tsx:34`                                     | Split-list search width                | keep with reason | Uses the existing search primitive, but lets only split-list callers grow it into the available left-column space; regular tab headers retain their compact width.                             | None.            |
| `src/modules/MainApp/WorkManagement/GitHubWorkItemsHeaderControls.tsx:99`                            | Split search/action row                | keep with reason | Its wrapper receives `flex-1` only in split mode, so the shared search primitive can consume the left-column remainder while the action group keeps its compact token spacing.                 | None.            |
| `src/modules/MainApp/WorkManagement/GitHubWorkItemControls.tsx:47`                                   | Issue filter button                    | keep with reason | Uses the design-system tertiary, small icon button and standard toolbar tooltip to match refresh and write actions in the shared header.                                                       | None.            |
| `src/modules/MainApp/TeamInbox/TeamInboxView.tsx:406`                                                | Inbox one-row split header             | keep with reason | Inbox keeps dataset, search, and compact actions in one left-column row without a divider whenever the surface is split, independent of tab-bar availability.                                  | None.            |
| `src/modules/MainApp/TeamInbox/components/TeamInboxList.tsx:156`                                     | Inbox search/action separation         | keep with reason | Reuses the shared search, tertiary actions, and toolbar tooltips, with `gap-1` before the icon group and one-pixel spacing inside it; no divider follows the search field.                     | None.            |
| `src/modules/MainApp/TeamInbox/components/TeamInboxList.tsx:216`                                     | Inbox section headings                 | keep with reason | Reuses the Workstation Trail section-label token inside the shared collapsible-section behavior, matching its compact typography while preserving grouping and keyboard semantics.             | None.            |
| `src/modules/ProjectManager/WorkItems/components/WorkItemsPageHeader/WorkItemsHeaderContent.tsx:112` | Work Item header cluster               | keep with reason | Uses the established status selector, search, refresh, and add primitives in the page header; search is adjacent to its actions without a trailing divider.                                    | None.            |
| `src/modules/ProjectManager/WorkItems/components/WorkItemsPageHeader/index.tsx:111`                  | Work Items split-header projection     | keep with reason | Projects the established page-header content into the shared two-row left header and asks the workstation shell to hide its duplicate row.                                                     | None.            |
| `src/modules/MainApp/WorkManagement/WorkManagementProjectsSurface.tsx:88`                            | Project/work-item split-header handoff | keep with reason | Passes the compact dataset switch to both project-scoped and workspace work-item surfaces without duplicating their split-state decision in the parent.                                        | None.            |
| `src/modules/ProjectManager/ProjectManagerLayout/components/ProjectWorkItemsTabContent.tsx:104`      | Workspace Work Items split gate        | keep with reason | Keeps the left-column header for every List split; Kanban and expanded List retain the normal page or tab header.                                                                              | None.            |
| `src/modules/ProjectManager/WorkItems/index.tsx:423`                                                 | Project Work Items split gate          | keep with reason | Keeps the existing header controls in the left list whenever it is split; expanded List and all other states retain their normal page or tab header.                                           | None.            |
| `src/modules/MainApp/WorkManagement/GitHubWorkItemsCompactList.tsx:64`                               | Compact PR/issue rows                  | keep with reason | Reuses `CompactListPanel` for selection, listbox semantics, and keyboard navigation; the PR-state data attribute does not create a separate visual primitive.                                  | None.            |
| `src/modules/shared/githubRepositoryLabel.ts:3`                                                      | Repository display labels              | keep with reason | Centralizes the owner-prefix removal so selects, tables, and compact Inbox metadata consistently show the complete repository name without changing the canonical repository identifier.       | None.            |
| `src/modules/ProjectManager/WorkItems/components/WorkItemsListSurface/index.tsx:151`                 | Unselected Work Item holder            | keep with reason | Composes `DetailPaneLayout` and `DetailPanePlaceholder`, the existing shared right-pane primitives, rather than duplicating pane chrome.                                                       | None.            |
| `src/modules/ProjectManager/WorkItems/components/EmbeddedWorkItemDetail/index.tsx:95`                | Embedded Work Item empty state         | keep with reason | Keeps the right-hand holder mounted with the same shared detail primitives before selection, so project-scoped work items match GitHub work surfaces.                                          | None.            |
| `src/modules/MainApp/TeamInbox/components/TeamInboxDetailPane.tsx:136`                               | Inbox placeholder holder               | keep with reason | Placeholder states deliberately render body-only detail panes: no synthetic header row and no close affordance before an Inbox item is selected.                                               | None.            |

Verdict totals: **0 fix**, **33 keep with reason**, **0 abstract**.
