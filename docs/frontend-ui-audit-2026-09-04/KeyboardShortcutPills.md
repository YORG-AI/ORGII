# Keyboard shortcut pills UI audit

| Line                                                             | Element                              | Verdict          | Reason                                                                                                                                                                                                                                | Suggested change                                                                 |
| ---------------------------------------------------------------- | ------------------------------------ | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| `src/components/KeyboardShortcut/index.tsx:272`                  | Shared compact shortcut presentation | keep with reason | This is the design-system primitive for shortcut hints; its 18px optical geometry is intentionally smaller than ordinary controls, and its colors use theme tokens.                                                                   | Keep the joined `kbd` capsule as the single source for compact shortcut styling. |
| `src/components/KeyBadge/index.tsx:278`                          | Settings shortcut badge              | keep with reason | Settings must parse alternatives, packed macOS modifiers, four arrow directions, and named keys that the compact tooltip component does not support. It uses the same joined-capsule visual rule at the table's larger control scale. | Keep the specialized parser, but retain one `kbd` per chord.                     |
| `src/scaffold/GlobalSpotlight/components/SpotlightFooter.tsx:71` | Navigation-arrow shortcut hint       | fix              | A hand-built `kbd` duplicated the shared shortcut component and could drift from the new joined-pill format.                                                                                                                          | Applied: render `up down` through `KeyboardShortcut` with the footer variant.    |
| `src/features/CodeMirror/SqlEditor/index.tsx:259`                | Run-query shortcut hint              | fix              | Two raw `kbd` elements duplicated the old split-key presentation and hardcoded the macOS binding.                                                                                                                                     | Applied: use `KeyboardShortcut` with the platform-aware `db_run_query` binding.  |

Verdict totals: **2 fix**, **2 keep with reason**, **0 abstract**.
