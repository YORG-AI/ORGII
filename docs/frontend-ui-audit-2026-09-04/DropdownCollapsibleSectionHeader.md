# DropdownCollapsibleSectionHeader UI audit

| Line                                                    | Element                       | Verdict          | Reason                                                                                                                                                        | Suggested change                                                                                        |
| ------------------------------------------------------- | ----------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------- |
| `DropdownCollapsibleSectionHeader.tsx:20`               | Section-title button          | keep with reason | This is the dropdown design-system primitive itself, so a native button is the correct semantic foundation and cannot be replaced by itself.                  | None.                                                                                                   |
| `DropdownCollapsibleSectionHeader.tsx:23`               | Title and chevron styling     | keep with reason | The component reuses `DROPDOWN_CLASSES.sectionLabel` and `DROPDOWN_ITEM.iconSize`; no raw color or arbitrary size is introduced.                              | None.                                                                                                   |
| `DropdownCollapsibleSectionHeader.tsx:28`               | Expansion semantics           | keep with reason | The full row is keyboard-operable through native button behavior, exposes `aria-expanded`, and marks the decorative chevron hidden from assistive technology. | None.                                                                                                   |
| `PortsStatusMenu.tsx:394` and `PortsStatusMenu.tsx:424` | Workspace and External titles | abstract         | The requested reusable seam is a dropdown-owned collapsible section header; both domain sections now supply only their content and state.                     | Extract to `DropdownCollapsibleSectionHeader` with the chevron immediately after the title (completed). |
| `*/common.json:workstation.ports.workspaceSection`      | Current-workspace label       | fix              | Every non-English catalog retained the untranslated English “Workspace,” and the label did not make its current-workspace scope explicit.                     | Use “Current workspace” in English and native wording in all 12 other supported locales (completed).    |

Verdict totals: **1 fix**, **3 keep with reason**, **1 abstract**.
