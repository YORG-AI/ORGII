# DropdownSearch UI audit

| Line                                                                    | Element                              | Verdict          | Reason                                                                                               | Suggested change                                                                                               |
| ----------------------------------------------------------------------- | ------------------------------------ | ---------------- | ---------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------- |
| `src/components/Dropdown/tokens.ts:188`                                 | Shared search and header row styling | fix              | Search rows inherited the titled-header divider because both roles used one bordered token           | Split the common row layout from the bordered header variant and keep `searchContainer` divider-free (applied) |
| `src/components/Dropdown/DropdownSearch.tsx:191`                        | Search row wrapper and native input  | keep with reason | This is the design-system primitive that centralizes the pattern for dropdown consumers              | None                                                                                                           |
| `src/components/Dropdown/DropdownHeader.tsx:27`                         | Titled panel header wrapper          | keep with reason | The wrapper preserves the intended divider for non-search headers through the dedicated header token | None                                                                                                           |
| `src/scaffold/ContextMenu/variants/TextSelectionDropdown/index.tsx:136` | Session selector back/title row      | fix              | A non-search header referenced the search token directly and would otherwise lose its divider        | Use the dedicated bordered header token (applied)                                                              |

Verdict totals: **2 fix**, **2 keep with reason**, **0 abstract**.
