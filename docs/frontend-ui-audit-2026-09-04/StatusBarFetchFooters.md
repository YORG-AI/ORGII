# StatusBar fetch footers UI audit

| Line                      | Element                                     | Verdict          | Reason                                                                                                                                                            | Suggested change                                                            |
| ------------------------- | ------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------- |
| `statusBarTokens.ts:47`   | Shared status-menu footer and clock styling | fix              | The neighbouring CI and Ports menus should not independently define footer spacing or timestamp typography.                                                       | Reuse `menuFooterClass` and `menuTimestampClass` in both menus (completed). |
| `CiStatusMenu.tsx:382`    | Pull-request header link                    | keep with reason | The native button is a compact, text-only action inside the shared dropdown header row; a general button surface would add inappropriate chrome.                  | None.                                                                       |
| `CiStatusMenu.tsx:420`    | CI refresh footer                           | keep with reason | It uses the shared dropdown footer, menu-action, refresh-animation, and status-menu typography tokens, with a visible accessible name and disabled state.         | None.                                                                       |
| `PortsStatusMenu.tsx:481` | Ports refresh footer                        | keep with reason | It uses the same shared footer and clock contract as CI while retaining the domain-specific rescan action.                                                        | None.                                                                       |
| `PortsStatusMenu.tsx:120` | Port-address width cap                      | keep with reason | The percentage cap is content-responsive within the fixed status panel and prevents the address from starving the process label; it is not a reusable fixed size. | None.                                                                       |

Verdict totals: **1 fix**, **4 keep with reason**, **0 abstract**.
