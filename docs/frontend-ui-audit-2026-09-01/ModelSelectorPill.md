# ModelSelectorPill UI audit

Scope: the combined model/effort pill, compact and advanced settings views, and the supporting shared dropdown changes. Earlier effort-slider work and unrelated working-tree changes are outside this audit.

| Line                                                         | Element                          | Verdict          | Reason                                                                                                                                                                                                                                                                                              | Suggested change |
| ------------------------------------------------------------ | -------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/components/ModelSelectorPill/index.tsx:270`             | Combined trigger                 | keep with reason | Reuses `SelectorPill` with its default transparent idle appearance, hover/active surface, and leading icon-to-chevron swap. No trailing chevron or separate effort segment for editable level families.                                                                                             | None             |
| `src/components/ModelSelectorPill/index.tsx:287`             | Effort label                     | keep with reason | Uses the existing purple token for Ultra and the secondary text token otherwise; the model and effort remain separately styled inside one button.                                                                                                                                                   | None             |
| `src/components/ModelSelectorPill/ModelSettingsMenu.tsx:154` | Positioned popup wrapper         | keep with reason | A stable panel ref preserves the shared dropdown engine's measurement, outside-click handling, and focus ownership across compact/advanced view changes. Width and z-index use dropdown tokens.                                                                                                     | None             |
| `src/components/ModelSelectorPill/ModelSettingsMenu.tsx:102` | Advanced choices                 | keep with reason | Reuses `DropdownItem` and `ActionSubmenu`; radio semantics expose selection, unavailable combinations are disabled, and descriptions can wrap. Optional flyout fitting preserves existing action-menu defaults.                                                                                     | None             |
| `src/components/ModelSelectorPill/ModelSettingsMenu.tsx:312` | Compact controls                 | keep with reason | Uses the default secondary `Button` for “Model settings”, the shared `IconButton` for Fast, and the existing native `EffortSlider`; no tertiary variant is used. Hiding the visual label retains the slider's accessible name/value and native keyboard/drag handling. Fast exposes `aria-pressed`. | None             |
| `src/components/ModelSelectorPill/index.tsx:311`             | Families without pickable levels | keep with reason | Keeps the existing `PillGroup` flow, including the model-only and speed-only variants, as explicitly requested.                                                                                                                                                                                     | None             |

Verdict totals: **0 fix**, **6 keep with reason**, **0 abstract**.

Verification: 45 targeted tests, changed-file ESLint, TypeScript typecheck, and `git diff --check` pass. Headless WebKit and Chromium exercised the production controls with fixture account data: native thumb drag/release outside, keyboard navigation, immediate changes, hover flyouts, light/dark themes, a 420px viewport, and focus restoration. This is component verification, not packaged Tauri or real-account persistence verification.

Architecture review covered compilation, component reuse, naming, effort/speed semantics, fallback branches, UI/domain boundaries, readability, shared entry-point parity, and variant resolver symmetry. Wire/schema/backend changes were intentionally excluded: the existing variant resolver and `useModelEffortSegment.handleApply` remain the authoritative selection/write path; no persistence migration or historical cleanup is introduced.

The compact entry is labeled “Model settings” and the return row “Back”, using existing translations. Rendered evidence and its limits are recorded in [ModelSettings verification](../verification-2026-09-01/ModelSettings.md).
