# MobileRemote settings and devices UI audit

| Line                                                                    | Element                             | Verdict          | Reason                                                                                                                                                                                                                                                                                                            | Suggested change                                                                                         |
| ----------------------------------------------------------------------- | ----------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------- |
| `src/modules/shared/layouts/SectionLayout/Row.tsx:30`                   | Always-inline structured row        | abstract         | Settings and device identity surfaces both need a narrow-screen label/value row, while the existing horizontal row intentionally stacks below 480px. Adding one browser-safe `inline` layout to the existing design-system owner removes the repeated Mobile-only row geometry without changing Desktop defaults. | Keep `inline` inside `SectionRow`; do not add a second Mobile settings-row primitive.                    |
| `src/modules/MobileRemote/screens/settings/SettingsTab.tsx:118`         | Connection settings group           | keep with reason | The screen now composes `SectionContainer`, `SectionRow`, and the shared muted-value typography token. Protocol values are mapped to localized product copy before rendering, and long relay URLs use the shared row's shrink boundary rather than an arbitrary 55% width.                                        | None.                                                                                                    |
| `src/modules/MobileRemote/screens/settings/SettingsTab.tsx:141`         | Optional help actions               | keep with reason | Help and destructive rows reuse `SectionContainer`, `SectionRow`, DS `Button`, and Hugeicons. They render only when an owning callback exists, so the Settings page no longer presents inert controls that look actionable.                                                                                       | Supply callbacks only when the pairing guide and confirmed revoke lifecycles are implemented end to end. |
| `src/modules/MobileRemote/screens/devices/DevicesTab.tsx:78`            | This-device summary                 | keep with reason | The former bespoke bordered card now uses `SectionContainer` and the shared inline row, with the canonical phone icon and value typography.                                                                                                                                                                       | None.                                                                                                    |
| `src/modules/MobileRemote/screens/devices/DevicesTab.tsx:110`           | Paired-desktop list                 | keep with reason | Each paired desktop now reuses structured-row separators, Hugeicons identity, and canonical `StatusDot` semantics instead of maintaining an independent rounded-card and status treatment. The empty state remains on the DS `Placeholder`.                                                                       | None.                                                                                                    |
| `src/modules/MobileRemote/screens/SessionsScreen.tsx:33`                | Session-list group heading          | keep with reason | The `Live` heading now consumes `LIST_PANEL_SECTION_HEADER.typography`, matching Desktop list-panel density without coupling Mobile to Desktop navigation behavior.                                                                                                                                               | None.                                                                                                    |
| `src/modules/MobileRemote/components/MobileTopBar.tsx:22`               | Mobile top bar                      | keep with reason | The bar already uses the DS `IconButton`, Hugeicons, semantic header markup, and design tokens. Its fixed compact height and touch navigation ownership are Mobile-specific rather than duplicated Desktop behavior.                                                                                              | None.                                                                                                    |
| `src/modules/MobileRemote/components/MobileTabBar.tsx:47`               | Mobile bottom navigation            | keep with reason | The bottom navigation already composes DS `Button` and Hugeicons, preserves safe-area padding, and owns a touch-first navigation pattern that Desktop does not share.                                                                                                                                             | None.                                                                                                    |
| `src/modules/MobileRemote/components/MobileShell.tsx:13`                | Narrow viewport and safe-area shell | keep with reason | The 393px cap is the documented Mobile baseline and the shell owns browser safe-area and viewport containment. Replacing it with a Desktop content container would regress the keyboard, bottom-composer, and centered desktop-preview behavior.                                                                  | None.                                                                                                    |
| `src/modules/MobileRemote/screens/WelcomeScreen.tsx:20`                 | Pairing welcome composition         | keep with reason | The one-off centered onboarding composition already uses DS `Button`, Hugeicons, and semantic heading/body text. Its illustration scale and vertical centering are a flow-specific empty/onboarding state, not a repeated settings pattern.                                                                       | None.                                                                                                    |
| `src/modules/MobileRemote/screens/QRScanScreen.tsx:42`                  | Pairing input screen                | keep with reason | The screen reuses `MobileTopBar`, DS `Textarea`, and DS `Button`; only the lifecycle-specific hint, validation placement, and full-width mobile layout remain local.                                                                                                                                              | None.                                                                                                    |
| `src/modules/MobileRemote/components/transcript/MobileToolCall.tsx:325` | Tool activity and detail trigger    | keep with reason | This area was already covered by the transcript audit: it reuses Desktop `EventBlockHeader`, shared Session tokens, canonical icons, shared `BottomSheet`, and shared Prism highlighting. A native button remains the accessible full-row dialog trigger rather than duplicating visual styling.                  | None.                                                                                                    |

Verdict totals: **0 fix**, **11 keep with reason**, **1 abstract**.
