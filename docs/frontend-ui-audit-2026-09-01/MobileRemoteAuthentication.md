# MobileRemote authentication UI audit

| Line                                                            | Element                                     | Verdict          | Reason                                                                                                                                                                                                            | Suggested change |
| --------------------------------------------------------------- | ------------------------------------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/modules/MobileRemote/auth/MobileAuthScreen.tsx:36`         | Authentication viewport shell               | keep with reason | Reuses `MobileShell`, which already owns the mobile safe-area, narrow-viewport, theme, and keyboard containment contract.                                                                                         | None.            |
| `src/modules/MobileRemote/auth/MobileAuthScreen.tsx:46`         | Authentication loading state                | keep with reason | Reuses the design-system `Placeholder` loading state and an `aria-live` region instead of introducing a second spinner or blank startup surface.                                                                  | None.            |
| `src/modules/MobileRemote/auth/MobileAuthScreen.tsx:56`         | Authentication heading and explanatory copy | keep with reason | Uses the existing text-color and typography tokens; the centered composition is a one-off gate state rather than a repeated card or form pattern.                                                                 | None.            |
| `src/modules/MobileRemote/auth/MobileAuthScreen.tsx:65`         | Authentication error state                  | keep with reason | Reuses `InlineAlert` for alert semantics, danger styling, and its retry action rather than rebuilding error chrome locally.                                                                                       | None.            |
| `src/modules/MobileRemote/auth/MobileAuthScreen.tsx:78`         | GitHub sign-in action                       | keep with reason | Reuses the design-system `Button` and canonical Hugeicons wrapper with a full-width mobile action layout.                                                                                                         | None.            |
| `src/modules/MobileRemote/screens/settings/SettingsTab.tsx:113` | Signed-in account section                   | keep with reason | Reuses `SectionContainer`, `SettingsValueRow`, and `SettingsActionRow`, preserving the same row geometry, truncation, tokenized typography, and destructive-action treatment as the other Mobile Settings groups. | None.            |

Verdict totals: **0 fix**, **6 keep with reason**, **0 abstract**.
