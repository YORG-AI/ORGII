# Kanban floating creator performance guard

| Area               | Verdict | Evidence                                                                                                                                    | Change or reason kept                                                                                                            | Verification                                                         |
| ------------------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------- |
| Background work    | keep    | When the shared pinned-actions preference is off, `LazyPinnedActionsBar` returns before loading the skills/actions component.               | The compact creator now follows the existing preference, avoiding its lazy skills chunk and discovery work while hidden.         | Pinned-actions lazy-loading test and compact visibility policy test. |
| Memory             | keep    | The close interaction uses one existing boolean atom plus a write-only toggle atom; it adds no collection, cache, queue, or retained model. | Shared state remains bounded to one boolean and is released with the existing application store lifecycle.                       | Two-click atom regression test and static ownership trace.           |
| Scope/isolation    | keep    | The persisted pinned-actions preference is intentionally shared by Launchpad and Session Creator surfaces.                                  | No project, account, organization, or viewer data is introduced or cached by this change.                                        | Source trace from shared setting to compact creator projection.      |
| Rendering/hot path | keep    | Visibility is a constant-time boolean projection evaluated during the existing creator render; no timer, poller, listener, or retry exists. | The creator button toggles on explicit clicks, and hidden pinned actions retain only the established inactive composer controls. | Focused tests, TypeScript check, and ESLint.                         |

Performance verdict: pass. No recurring work, cache, subscription, or unbounded state was added; disabling the setting now prevents compact-creator skills loading. Live CPU/RSS measurement was not run because Computer Use was not authorized and no idle runtime path was introduced.
