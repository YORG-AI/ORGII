# Creator composer surfaces UI audit

Acceptance: Agent and Manual creators share their input width and edge treatment, only the property/skills row scrolls horizontally, comments reuse the same soft shadow, and original input colors and focus rings remain intact.

| Line                                                                                         | Element                               | Verdict          | Reason                                                                                                                                                 | Suggested change |
| -------------------------------------------------------------------------------------------- | ------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| `src/components/ComposerShell/index.tsx:44`                                                  | Default, embedded, and comment shells | keep with reason | One shared pseudo-element consumes the existing soft dropdown shadow token; quiet history edits retain their existing treatment                        | None             |
| `src/styles/_utilities.scss:166`                                                             | Edge shadow and bottom glow           | keep with reason | The neutral edge uses the Workstation trail token; the primary gradient stays 90% wide and fades to transparent. Both decorations ignore pointer input | None             |
| `src/features/SessionCreator/variants/ChatPanel/index.scss:152`                              | Grouped creator surface               | keep with reason | The outer group owns the shadow and glow; the inner input retains its original fill and focus/drag ring without duplicate decoration                   | None             |
| `src/modules/ProjectManager/shared/components/CreateComposerScaffold.tsx:121`                | Manual composer width                 | keep with reason | Reuses ChatPanel width and horizontal gutter tokens instead of separate detail-panel widths                                                            | None             |
| `src/engines/ChatPanel/InputArea/components/PinnedActionsBar/index.tsx:418`                  | Creator property/skills row           | keep with reason | One bounded horizontal scroller contains the controls, while the editor and repository toolbar remain outside it                                       | None             |
| `src/modules/ProjectManager/shared/components/PropertiesPanel/ProjectPropertyFields.tsx:417` | Compact project properties            | keep with reason | Create Project keeps status and priority inline and uses the same circular design-system More button; hidden fields remain editable in its menu        | None             |

Verdict totals: **0 fix**, **6 keep with reason**, **0 abstract**.

D1–D5 reviewed. This layer changes presentation only; no persistence, listeners, or request owners are added. The broader shared-token change is explicitly included in the requested creator refactoring series. Targeted tests cover field accessibility and retained values; isolated headless checks compare themes, widths, focus rings, and outer/inner shadow ownership. These fixtures do not substitute for native Tauri interaction or performance measurements.
