# Project pane ownership UI audit

| Line                                                                     | Element                                      | Verdict          | Reason                                                                                                                                                                                                                | Suggested change                                |
| ------------------------------------------------------------------------ | -------------------------------------------- | ---------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- |
| `src/engines/ChatPanel/panels/ProjectPanelView.tsx:164`                  | Sticky overview/list/Kanban panels and shell | keep with reason | Existing `PersistentDetailTabPanel`, `DetailPanelContainer`, and `WorkItemContentStack` preserve IDs, classes, lazy mount, hidden-tab state, and overflow behavior.                                                   | None; original/refactored DOM contract matches. |
| `src/engines/ChatPanel/panels/projectPanel/useProjectOverview.tsx:111`   | Overview editor and load/error states        | keep with reason | Existing `ProjectContentEditor`, `ChatLoadingBlock`, and `Placeholder` remain unchanged. Raw section/div elements provide semantic/layout wrappers, not replacement controls.                                         | None.                                           |
| `src/engines/ChatPanel/panels/projectPanel/useProjectProperties.tsx:220` | Properties rail dimensions and collapse icon | keep with reason | Existing 300/280/320 rail bounds and 14px icon are intentional surface geometry. Changing them during ownership extraction would change user experience. DS rail, field, button, and tooltip components are retained. | None.                                           |
| `src/engines/ChatPanel/panels/projectPanel/useProjectWorkItems.tsx:324`  | Search/status/grouping controls              | keep with reason | Existing Button, ToolbarTooltip, TabPill, status select, aria labels, and selected-state token classes remain in the same order and wrapper. No new arbitrary Tailwind values or literal colors.                      | None.                                           |
| `src/engines/ChatPanel/panels/projectPanel/useProjectWorkItems.tsx:266`  | List/Kanban wrappers and footer              | keep with reason | Existing domain components already own repeated rows/cards and bulk controls. The pane continues to pass the same readonly, compact, permission, drag, and grouping configuration.                                    | None; no new design-system abstraction.         |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.

The five dimensions were checked on all changed TSX, including the aggregate interaction hook (no JSX change). No multi-file design-system sweep candidate was introduced. Six pane/leaf-prop snapshots and action contracts pass for both the original pane and the refactor. Heavy widgets are mocked in that check: it is not a pixel screenshot or native-app validation. No visual redesign is intended, and desktop control was not authorized.
