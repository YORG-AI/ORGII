# FileHeaderMoreMenu UI audit

| Line                                                             | Element                   | Verdict          | Reason                                                                                                                                                                                                                   | Suggested change |
| ---------------------------------------------------------------- | ------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| `FileHeaderMoreMenu.tsx:250`                                     | Main actions menu         | keep with reason | Uses the shared `ActionMenuSurface`, dropdown panel/width tokens, and the existing file actions. Display controls now live in the explicitly requested second layer.                                                     | None.            |
| `FileHeaderMoreMenu.tsx:270`                                     | Action rows and shortcuts | keep with reason | Uses `DropdownItem` with menu semantics, keyboard focus, disabled states, and the existing `KeyboardShortcut` component.                                                                                                 | None.            |
| `FileHeaderMoreMenu.tsx:414`                                     | UI settings submenu       | keep with reason | Reuses the session menu's flyout implementation from the design-system layer; hover, keyboard navigation, and Escape behavior have one owner.                                                                            | None.            |
| `FileHeaderMoreMenu.tsx:164`                                     | Toggle row wrapper        | keep with reason | The existing row-wide toggle target wraps the design-system `Switch`; checked/disabled state, Enter/Space activation, and event propagation are explicit. Switches now have accessible names for submenu keyboard focus. | None.            |
| `ActionMenuSurface.tsx:157`                                      | Flyout geometry           | keep with reason | Position derives from measured bounds and existing dropdown padding/gap tokens. The padded bridge keeps pointer movement between panels usable without a timer.                                                          | None.            |
| `FileHeaderMoreMenu.tsx:419`, `SessionHeaderActionsMenu.tsx:497` | UI settings icon          | keep with reason | Both use the requested `Layers01Icon`, the same dropdown icon-size token, and the same stroke width. No new color or size literals are introduced.                                                                       | None.            |
| `FileHeaderMoreMenu.tsx:415`, `SessionHeaderActionsMenu.tsx:494` | Shared label              | keep with reason | Both use `common:actions.uiSettings`. All 13 translations were moved unchanged from the session-specific key, which was removed.                                                                                         | None.            |

Verdict totals: **0 fix**, **7 keep with reason**, **0 abstract**.

Scope: the file-header menu, session-menu integration, and extracted shared flyout. No additional design-system sweep candidates were found within this change. The shared extraction replaces the existing implementation rather than adding a second copy.

Verification: 42 tests passed across `FileHeaderMoreMenu.test.ts`, `SessionHeaderActionsMenu.test.ts`, and `Dropdown/index.test.ts`. These cover menu nesting, enabled/disabled controls, callbacks, keyboard traversal, staged Escape dismissal, hover bridging, geometry, and repeated-close cleanup. Scoped ESLint passed. All 13 locale values were compared with the original translations.

Desktop screenshots, themes, and actual viewport rendering were not verified: the user's computer-control preference is opt-in, and no such opt-in was provided. No visual-perfectness claim is made.
