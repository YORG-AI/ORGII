# SidebarSettingsMenuButton UI audit

Scope: remove ADE Manager and Tutorials from the shared account menu; show Logout first when signed in and Login last when signed out. Login and Logout use Login02Icon and Logout02Icon; View RAM shares the main options group. The highlighted Settings return button is unchanged. Both the Settings and workstation sidebars consume this menu.

| Line                                                                      | Element       | Verdict          | Reason                                                                                                                                                                                          | Suggested change |
| ------------------------------------------------------------------------- | ------------- | ---------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/scaffold/NavigationSidebar/blocks/SidebarSettingsMenuButton.tsx:339` | Logout action | keep with reason | Uses the shared dropdown action classes, icon size, semantic colors, native button semantics, and existing translated `cloud.signOut` label. The authoritative auth atom determines visibility. | None.            |
| `src/scaffold/NavigationSidebar/blocks/SidebarSettingsMenuButton.tsx:361` | Menu grouping | keep with reason | View RAM sits directly with Presence, Appearance, and Layout, without a separate section. Account-action separators remain conditional.                                                         | None.            |
| `src/scaffold/NavigationSidebar/blocks/SidebarSettingsMenuButton.tsx:484` | Login action  | keep with reason | Uses the same existing dropdown treatment and supplied login handler; the signed-out guard prevents simultaneous Login and Logout rows, and DOM order places Login last.                        | None.            |

Verdict totals: **0 fix**, **3 keep with reason**, **0 abstract**.

## Lifecycle review

| Area               | Verdict | Evidence                                                                                                                                                                                         | Change or reason kept                                                               | Verification                                                                                                                           |
| ------------------ | ------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ----------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------- |
| Background work    | keep    | Logout calls `resetOrgEntitlementCoordinator(store)` before clearing auth, matching `Org2CloudSection`. The coordinator clears deferred timers and invalidates pending responses by store epoch. | No new timer, retry, worker, or network request.                                    | Menu logout test checks the owning store is reset; coordinator tests exercise late responses and retry cancellation.                   |
| Memory             | keep    | One existing auth-atom subscription and no new retained collection or cache. The menu closes before logout.                                                                                      | Shared Jotai storage and dropdown teardown remain responsible for disposal.         | Source review of the existing subscription/cleanup paths and menu close assertion.                                                     |
| Scope/isolation    | keep    | Logout writes `null` through `org2CloudAuthAtom`, including its persisted storage path; entitlement reset is scoped to the same Jotai store.                                                     | Preserve the existing Settings logout semantics without adding a second auth store. | Rendered component test verifies both the atom and serialized auth are cleared, Logout disappears, and Login becomes the final action. |
| Rendering/hot path | keep    | Auth updates reach the small shared menu; there is no session-stream subscription, scan, or sorting work.                                                                                        | Existing dropdown positioning and idle behavior are unchanged.                      | Source review; no measured performance claim.                                                                                          |

Lifecycle coverage: opening Settings or the workstation mounts one active sidebar; closed/idle and hidden states gain no periodic work; leaving the route removes the menu subscription; logout closes menu/submenus and invalidates account-owned pending entitlement work. Online/offline logout remains the existing synchronous local-auth operation. This change does not claim browser-session revocation or changes to other app instances.

## Verification

- `pnpm exec vitest run src/scaffold/NavigationSidebar/blocks/SidebarSettingsMenuButton.test.ts src/scaffold/NavigationSidebar/connectors/__tests__/SidebarAccountButton.test.ts src/features/Org2Cloud/org2CloudAuthAtom.test.ts src/features/Org2Cloud/org2CloudEntitlementCoordinator.test.ts src/features/Org2Cloud/useOrg2CloudSignIn.test.ts src/features/Org2Cloud/org2CloudAuthLoopback.test.ts` — 48 tests passed across six files.
- `pnpm exec tsc --noEmit --pretty false --incremental --tsBuildInfoFile .git/.tsbuildinfo` — passed in the isolated PR checkout. The unrelated untracked test failures from the original working directory are not part of this branch.
- `pnpm exec eslint src/icons.ts src/scaffold/NavigationSidebar/blocks/SidebarSettingsMenuButton.tsx src/scaffold/NavigationSidebar/blocks/SidebarSettingsMenuButton.test.ts src/scaffold/NavigationSidebar/connectors/SidebarAccountButton.tsx src/scaffold/NavigationSidebar/variants/SettingsSidebar.tsx --max-warnings 0` — passed.
- `pnpm exec prettier --check src/icons.ts src/scaffold/NavigationSidebar/blocks/SidebarSettingsMenuButton.tsx src/scaffold/NavigationSidebar/blocks/SidebarSettingsMenuButton.test.ts src/scaffold/NavigationSidebar/connectors/SidebarAccountButton.tsx src/scaffold/NavigationSidebar/variants/SettingsSidebar.tsx docs/frontend-ui-audit-2026-08-31/SettingsSidebar.md docs/frontend-ui-audit-2026-08-31/SidebarSettingsMenuButton.md` — passed.
- `git diff --check` — passed.
- Real-app visual checks, live login/logout, and CPU/RAM measurements were not run. Computer Use was not authorized. The menu regression test uses an isolated store; its persistence assertion reads the test storage, not a real account.

Performance verdict: pass for the scoped lifecycle invariants supported by source review, targeted tests, lint, and compilation. No measured CPU/RAM or live OAuth claim.
