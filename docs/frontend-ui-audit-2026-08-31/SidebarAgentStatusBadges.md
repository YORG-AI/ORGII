# Sidebar agent status badges UI audit

| Line                                                                                                | Element                           | Verdict          | Reason                                                                                                                                                                                                                                   | Suggested change |
| --------------------------------------------------------------------------------------------------- | --------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/scaffold/NavigationSidebar/components/NavigationMenu/NavigationMenu/NavigationMenuRow.tsx:467` | Leading icon and status badge     | keep with reason | One shared renderer serves leaf and parent rows, including collapsed rows. The 14px icon box uses standard spacing tokens; the badge is absolutely positioned and reserves no trailing column.                                           | None.            |
| `src/scaffold/NavigationSidebar/components/NavigationMenu/NavigationMenu/NavigationMenuRow.tsx:498` | Badge backing and separation ring | keep with reason | Existing `bg-bg-1` and `ring-bg-1` theme tokens separate the status dot from either a brand mark or glyph. The pointer-transparent badge does not intercept row or icon actions, and does not inherit the trailing accessory hover fade. | None.            |
| `src/scaffold/NavigationSidebar/connectors/useSessionMenuItems/menuItemBuilders.tsx:101`            | Session status                    | keep with reason | Reuses the canonical status derivation and existing accessible labels/colors. Only placement changes; pin and Git indicators retain their trailing slot.                                                                                 | None.            |
| `src/scaffold/NavigationSidebar/connectors/useProjectsWorkItemMenuItems/menuRows.tsx:351`           | Linked-session status             | keep with reason | Uses the same icon badge slot while preserving translated status labels, colors, and running-state presentation. Work-item pending-sync indicators are a separate concern and remain unchanged.                                          | None.            |
| `src/scaffold/NavigationSidebar/connectors/workstationSidebarMenuItems.tsx:216`                     | Draft status                      | keep with reason | The existing outlined dot uses the shared icon badge slot and existing border/size tokens. Expanded subagent rows continue to omit read-status markers.                                                                                  | None.            |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.

Architecture coverage: layers 1–7 cover the UI contract, shared renderer, consumers, naming, defaults, and status ownership. Layers 8–10 do not apply: no wire, initialization, or multi-source resolver change. Existing working-status derivation, subagent exclusions, project sync indicators, pin/Git controls, and hover actions are preserved. No timers, subscriptions, or retained state were added or modified.

Desktop visual checks were not run because Computer Use was not authorized. Visual contrast and hover/collapsed behavior remain unverified in the desktop app. Automated verification results are recorded in the pull request.

Verification in the isolated PR branch: 87 tests passed; scoped ESLint passed with zero warnings; `pnpm run typecheck` passed; `git diff --check` passed.
