# WorkItemPickerModal UI audit

| Line                                                                                     | Element                         | Verdict          | Reason                                                                                                                                                                                                                                                           | Suggested change |
| ---------------------------------------------------------------------------------------- | ------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/features/SessionCreator/components/WorkItemPickerModal/index.tsx:130`               | Reusable dialog shell           | keep with reason | Uses the canonical Modal, large preset, standard header/footer, localized Add/Cancel, and an optional initial focus ref; the consumer receives selected options without any composer dependency                                                                  | None             |
| `src/features/SessionCreator/components/WorkItemPickerModal/WorkItemPickerPanel.tsx:120` | Modal body geometry             | keep with reason | Retains the previous 520px list budget with a 60vh cap; flex shrinking and the shared modal viewport bounds keep the search/header/footer separate from the scrolling result list. An 800px preferred width accommodates item titles without growing the creator | None             |
| `src/features/SessionCreator/components/WorkItemPickerModal/WorkItemPickerPanel.tsx:123` | Search and refresh              | keep with reason | Reuses SearchInput and small Button; refresh remains explicit, labeled, and disabled during a request. Search receives initial focus through the shared modal instead of competing focus effects                                                                 | None             |
| `src/features/SessionCreator/components/WorkItemPickerModal/WorkItemPickerPanel.tsx:155` | Source filters                  | keep with reason | Preserves the existing four source tabs and container-responsive labels; icon-only tabs retain accessible names and selected state                                                                                                                               | None             |
| `src/features/SessionCreator/components/WorkItemPickerModal/WorkItemPickerPanel.tsx:236` | Item rows and selection         | keep with reason | Keeps shared list tokens, mini Checkbox, AnyIcon, PR status, and PrCiStatusIndicator; the local spacing override preserves the screenshot's dense two-line rows after removing composer-scoped CSS                                                               | None             |
| `src/features/SessionCreator/variants/ChatPanel/WorkItemAttachmentControl.tsx:147`       | Card, pill, and button triggers | keep with reason | Existing LaunchpadActionCard/Button presentations open the same modal with dialog semantics; Link keeps its existing small menu and direct Create navigation remains unchanged                                                                                   | None             |
| `src/scaffold/ModalSystem/index.tsx:255`                                                 | Focus and dismissal             | keep with reason | One shared owner handles focus trapping, returning focus, Escape, and the backdrop. It now skips disabled controls, refreshes the Tab boundary when Add becomes enabled, and cancels delayed focus on close                                                      | None             |
| `src/features/SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:430`     | Stable creation layout          | keep with reason | Removes expanded-picker state, portal host, hidden suggestions, and associated CSS; repository trail, skills row, cards/pills, and editor keep their existing layout while the modal is open                                                                     | None             |

Verdict totals: **0 fix**, **8 keep with reason**, **0 abstract**.

Reviewed D1–D5. The user explicitly requested this reusable modal, so extracting the selection boundary is authorized even though the audit's optional 3-occurrence abstraction threshold is not relevant. No independent design-system sweep was introduced. Colors, row typography, icon sizes, and PR metadata are preserved. The shared Modal changes support this dynamic picker: the actual mask now dismisses through the existing maskClosable guard, a requested field can receive initial focus, pending focus is canceled on unmount, and Tab uses currently enabled controls.

Stack verification: 49 tests passed across eight focused suites, including the real shared Modal in jsdom. Tests exercise filtering and selection across sources, loading/errors/retry, stale results after close, repository changes, focus restoration, keyboard boundaries, and timer cleanup. The removed top-row Add work item control is replaced by the existing Solve Work Item entry; the reusable attachment component still supports direct-create and link callbacks. Native Tauri geometry and CPU/RSS are not measured.
