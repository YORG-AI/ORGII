# CreatorComposerPosition UI audit

| Line                                                                                    | Element                     | Verdict          | Reason                                                                                                                                                                                                                                                       | Suggested change |
| --------------------------------------------------------------------------------------- | --------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | ---------------- |
| `src/engines/ChatPanel/components/NewChatHeaderActionsMenu.tsx:105`                     | Compact menu controls       | keep with reason | Uses `menuControlItem`, `SegmentedTextPill size="small"` for Input/Trail, and `Switch size="small"` for CLI updates, matching the sidebar Layout menu shown in the user's screenshot; shared controls provide native keyboard semantics and accessible state | None             |
| `src/modules/shared/layouts/blocks/CreatorContentLayout.tsx:56`                         | Manual creator placement    | keep with reason | Auto margins and standard spacing center Work Item and Project content in normal flow; scrolling remains available when the content exceeds the viewport                                                                                                     | None             |
| `src/features/SessionCreator/variants/ChatPanel/index.tsx:108`                          | Agent creator placement     | keep with reason | All launchpad entry points read the same preference and reuse existing centered-composer geometry; there is no second editor or duplicated layout implementation                                                                                             | None             |
| `src/features/TeamCollaboration/components/CreateCollabOrgView/index.tsx:284`           | Organization form           | keep with reason | Keeps the shared SectionContainer and adds standard auto-margin utilities; source/mode selection cards are required form controls and remain unchanged while suggestion actions gain a pill presentation                                                     | None             |
| `src/modules/ProjectManager/Projects/components/GitHubIssuesImportWizard/index.tsx:205` | GitHub import form and hint | keep with reason | Reuses SectionContainer and standard spacing, centering the form together with its optional repository hint without replacing inputs                                                                                                                         | None             |
| `src/modules/shared/layouts/blocks/CreatorContentLayout.tsx:71`                         | Draft input ancestry        | keep with reason | Bottom/Middle updates classes on the existing wrapper; regression tests assert DOM identity and edited draft values survive both transitions for Work Item and Project layouts                                                                               | None             |
| `src/modules/ProjectManager/shared/components/CreateComposerScaffold.tsx:130`           | Manual skills/actions row   | keep with reason | Reuses the existing shared row and spacing above the input in Bottom and Middle; it does not subscribe to the separate repository trail preference                                                                                                           | None             |
| `src/features/SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:589`    | Agent skills/actions row    | keep with reason | Reuses PinnedActionsBar above the input in launchpad and other creator layouts; existing feature-visibility conditions remain intact; picker expansion is superseded by the reusable modal                                                                   | None             |
| `src/features/SessionCreator/components/LaunchpadActionGrid.tsx:85`                     | Compact suggestion pills    | keep with reason | Extends the existing shared pill presentation with intrinsic width, standard spacing, the existing tone palette, and a wrapping row; native button semantics and action callbacks remain shared                                                              | None             |
| `src/features/SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:659`    | Bottom suggestion row       | keep with reason | Uses the existing content width and normal flex flow outside the centered input stack, so wrapping consumes space instead of overlapping the editor; Bottom keeps the original card position                                                                 | None             |

Verdict totals: **0 fix**, **10 keep with reason**, **0 abstract**.

Reviewed D1–D5. The user's explicit all-surfaces request authorizes the common preference and form-layout updates. Existing composer glow, inline-alert, and backing-surface edits were preserved. Middle now renders existing suggestion actions as compact pills at the bottom; Bottom retains cards beneath the hero. Required runner, organization, and form controls remain available. Creators that did not have suggestion cards do not gain unrelated utility actions.

The Trail control shares the existing repository/location preference and native context-menu write path. Changing Input dispatches one shared state action that resets Trail to Up for Bottom and Down for Middle, even after an explicit choice. A later Trail override persists until the next layout change; reselecting the current layout leaves it intact. Skills/actions rows stay Up in both Agent and Manual independently of the trail. Manual Work Item and Project property/action rows are not repository trails, so they no longer consume that setting. Forms with no trail need no additional row. All eight menu labels are present in all 13 locales.

Integrated stack verification is recorded in CreatorRefactorStack.md in the architecture-audit directory. Native Tauri interaction is not claimed; the checked-in screenshots are isolated creator layout fixtures with editor, icon, and domain-service leaves stubbed. They exercise the real creator view/scaffold and compiled production styles.
