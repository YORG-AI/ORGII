# Kanban floating creator UI audit

| Line                                                                                      | Element                                   | Verdict          | Reason                                                                                                                                                                                 | Suggested change |
| ----------------------------------------------------------------------------------------- | ----------------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/features/TaskKanban/index.tsx:324`                                                   | New Session toggle action                 | keep with reason | Reuses the shared Work Management creator visibility state, so the existing highlighted header button now opens and closes the same floating creator without adding local UI state.    | None.            |
| `src/features/SessionCreator/variants/ChatPanel/SessionCreatorChatPanelView.tsx:223`      | Compact creator pinned-actions projection | keep with reason | Projects the persisted pinned-actions preference into the shared creator view instead of introducing a Kanban-only control or duplicating the skills bar.                              | None.            |
| `src/features/SessionCreator/variants/ChatPanel/repoChromeLayout.ts:27`                   | Pinned-actions visibility policy          | keep with reason | Keeps the compact Kanban surface aligned with Launchpad while preserving forced visibility on non-compact surfaces that do not expose the repository chrome restore control.           | None.            |
| `src/engines/ChatPanel/InputArea/components/PinnedActionsBar/LazyPinnedActionsBar.tsx:60` | Hidden pinned-actions state               | keep with reason | Reuses the established inactive row and lazy-loading boundary, so hiding skills preserves other composer controls and does not mount a second bespoke placeholder or settings surface. | None.            |

Verdict totals: **0 fix**, **4 keep with reason**, **0 abstract**.
