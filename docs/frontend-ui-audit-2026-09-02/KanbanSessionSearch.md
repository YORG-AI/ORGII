# Kanban header controls UI audit

| Line                                | Element                            | Verdict          | Reason                                                                                                                                                                                                     | Suggested change |
| ----------------------------------- | ---------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `KanbanSearchInput.tsx:13`          | Header search control              | keep with reason | Reuses `WorkManagementSearchInput`, which centralizes the shared pane-style `SearchInput`, clear behavior, width, and accessible label for four Work Management surfaces.                                  | None.            |
| `WorkManagementSearchInput.tsx:32`  | Responsive search width            | keep with reason | The fixed base width preserves the compact header pattern while the viewport-relative cap prevents right-side control clusters from overflowing narrow panes; no shared toolbar-search width token exists. | None.            |
| `useTaskKanbanHeader.tsx:53`        | Right-aligned control cluster      | keep with reason | The Workstation header API accepts React nodes per slot, so this lightweight flex wrapper is the required composition seam for keeping one filter trigger beside the trailing search control.              | None.            |
| `KanbanHeaderFilters/index.tsx:256` | Filter trigger                     | keep with reason | Uses the shared tertiary icon-only `Button`, shared header icon sizing, a localized accessible name, and pressed/expanded state instead of introducing another toolbar control style.                      | None.            |
| `KanbanHeaderFilters/index.tsx:286` | Filter menu surface                | keep with reason | Reuses `ActionMenuSurface`, shared dropdown geometry/tokens, and the dropdown engine used by chat-pane header menus, preserving the established overlay and keyboard model.                                | None.            |
| `KanbanHeaderFilters/index.tsx:299` | Second-level filter groups         | keep with reason | Agent, Auto archive, and Range use the shared `ActionSubmenu` pattern with their active values visible in the parent menu, matching the chat-pane “…” information hierarchy.                               | None.            |
| `KanbanHeaderFilters/index.tsx:315` | Filter option rows                 | keep with reason | Uses shared `DropdownItem` radio-menu semantics, focusability, selected styling, and checks consistently across all three filter groups.                                                                   | None.            |
| `ListView/index.tsx:137`            | List table without internal search | keep with reason | Uses the shared `SessionTable` configuration API to suppress its local search while retaining the table's sorting, pagination, and accessibility behavior.                                                 | None.            |
| `TaskKanbanContent/index.tsx:62`    | Filtered empty-state overlay       | keep with reason | This is a transient full-bleed message shared by board and list before either view mounts; the raw non-interactive wrapper provides only positioning and uses theme text tokens.                           | None.            |

Verdict totals: **0 fix**, **9 keep with reason**, **0 abstract**.
