# Composer menus UI audit

| Line                                                                                     | Element                               | Verdict          | Reason                                                                                                                                                                 | Suggested change |
| ---------------------------------------------------------------------------------------- | ------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/engines/ChatPanel/InputArea/components/ContextMenuPortal.tsx:101`                   | Sidebar-aware file preview placement  | keep with reason | Uses the shared sidebar atom through `usePathTreePosition` and the shared floating-position engine instead of duplicating layout state in each host.                   | None.            |
| `src/engines/ChatPanel/InputArea/components/SlashCommandPortal/SlashCommandMenu.tsx:209` | Skill path preview sidecar            | keep with reason | Reuses `FileTreePreview` and `DROPDOWN_PANEL.submenuGap`; the conditional logical side is computed state that a static utility class cannot express.                   | None.            |
| `src/features/SessionCreator/components/WorkItemPickerModal/index.tsx:183`               | Work Item picker dialog               | keep with reason | Reuses `SpotlightShell` and the shared picker panel, including Escape dismissal, focus restoration, and an accessible dialog label.                                    | None.            |
| `src/features/SessionCreator/components/WorkItemPickerModal/WorkItemPickerPanel.tsx:239` | Search, filters, results, and actions | keep with reason | Reuses `PaletteBody`, `SpotlightTabs`, and `SpotlightPinnedActionSection`; the dedicated composer entry suppresses the filter strip when only one source is available. | None.            |
| `src/scaffold/ContextMenu/index.tsx:302`                                                 | Shared composer command menu          | keep with reason | Reuses dropdown panel, item, section-label, hover, and selected-state tokens while preserving the menu's centralized keyboard navigation.                              | None.            |
| `src/scaffold/ContextMenu/MenuSections.tsx:381`                                          | File and Session section title        | keep with reason | Uses `DROPDOWN_CLASSES.sectionLabel`, matching the established compact dropdown section-header treatment without rebuilding a bespoke header.                          | None.            |

Verdict totals: **0 fix**, **6 keep with reason**, **0 abstract**.
