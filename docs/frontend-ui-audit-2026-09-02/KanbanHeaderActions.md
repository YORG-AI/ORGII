# Kanban header actions UI audit

| Line                                                            | Element                              | Verdict          | Reason                                                                                                                                                                                 | Suggested change |
| --------------------------------------------------------------- | ------------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------- |
| `src/features/TaskKanban/components/KanbanSearchInput.tsx:24`   | Collapsed search trigger             | keep with reason | Reuses the design-system tertiary icon-only `Button`, the shared search icon token, and an accessible label; expanding swaps it for the established Work Management search field.      | None.            |
| `src/features/TaskKanban/components/KanbanSearchInput.tsx:50`   | Expanded autofocused search          | keep with reason | Reuses `WorkManagementSearchInput`, its existing `onClose` seam, and a typed input ref instead of rebuilding focus, clear, and close behavior locally.                                 | None.            |
| `src/features/TaskKanban/hooks/useTaskKanbanHeader.tsx:61`      | Search/separator/filter/create group | keep with reason | Uses the shared separator, filter control, tertiary icon-only button, sidebar-matching `MessageAdd02Icon`, and workstation icon-size token; the order is covered by a regression test. | None.            |
| `src/features/TaskKanban/index.tsx:355`                         | Canonical creator action wiring      | keep with reason | The header action invokes the existing creator flow, remains mounted while the creator is visible, and receives the standard selected highlight with `aria-pressed` state.             | None.            |
| `src/modules/shared/components/WorkManagementSearchInput.tsx:5` | Optional search input controls       | keep with reason | Optional typed `inputRef` and `onClose` props expose existing design-system search behavior without changing defaults for GitHub Issues, Projects, or Work Items consumers.            | None.            |

Verdict totals: **0 fix**, **5 keep with reason**, **0 abstract**.
