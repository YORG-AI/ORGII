# Kanban replay removal UI audit

| Line                                             | Element                         | Verdict          | Reason                                                                                                               | Suggested change                                                                                                           |
| ------------------------------------------------ | ------------------------------- | ---------------- | -------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------- |
| `hooks/useTaskKanbanHeader.tsx:72`               | Header New Session button       | keep with reason | The action uses the shared `Button` component's small tertiary icon treatment and the standard header icon token.    | None.                                                                                                                      |
| `TaskDetailPanel/TaskDetailHeaderActions.tsx:51` | Merge and discard controls      | keep with reason | All persistent header actions use the shared `Button` component with labels, disabled states, and loading treatment. | None.                                                                                                                      |
| `TaskDetailPanel/TaskDetailHeaderActions.tsx:87` | Merge-strategy popover          | fix candidate    | The existing custom popover uses raw buttons and click-outside handling instead of shared menu primitives.           | In a separate UI cleanup, migrate the strategy choices to `ActionMenuSurface`/`DropdownItem` with menu keyboard semantics. |
| `TaskDetailPanel/TaskDetailViewPill.tsx:35`      | Transcript/touched-files toggle | keep with reason | The view switch already uses the shared `TabPill` component and contains no replay-specific visual branch.           | None.                                                                                                                      |

Verdict totals: **1 fix candidate**, **3 keep with reason**, **0 abstract**.
