# IosRemoteSharedUi UI audit

| Line                      | Element                                         | Verdict          | Reason                                                                                                                                                                                                                                   | Suggested change                                                                                                                                                                                              |
| ------------------------- | ----------------------------------------------- | ---------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `DevicesTab.tsx:128`      | Paired-desktop switch control                   | fix              | A raw `<button>` bypassed the shared focus, pressed, disabled, and visual treatments, and its hit area was smaller than the mobile row.                                                                                                  | Resolved with the shared tertiary ghost `Button`, full label-cell width, a 44px minimum hit area, and `aria-current` for the active desktop.                                                                  |
| `DevicesTab.tsx:132`      | Asynchronous desktop switch feedback            | fix              | The rejection was discarded and repeated taps could issue duplicate switch intents without pending or failure feedback.                                                                                                                  | Resolved with owner-local pending state, disabled concurrent selection, `aria-busy`, the shared loading presentation, and a retryable `InlineAlert` error while leaving connection ownership in the provider. |
| `DevicesTab.tsx:80`       | Mobile devices scroll surface                   | keep with reason | Uses the established Mobile Remote spacing scale (`px-4`, `py-4`, `gap-5`) and relies on the existing shell/tab-bar ownership for viewport and safe-area layout; there are no arbitrary Tailwind bridge values or hardcoded colors here. | None.                                                                                                                                                                                                         |
| `DevicesTab.tsx:82`       | This-device summary                             | keep with reason | Reuses `SectionContainer`, `SectionRow`, `HugeiconsIcon`, and `SECTION_VALUE_SMALL_MUTED_CLASSES`; the layout and typography remain aligned with the shared settings surfaces.                                                           | None.                                                                                                                                                                                                         |
| `DevicesTab.tsx:110`      | Paired-desktop section and empty state          | keep with reason | Reuses `SectionContainer` and the shared `Placeholder` empty variant instead of rebuilding card or empty-state chrome. Standard Tailwind spacing is used rather than arbitrary pixel values.                                             | None.                                                                                                                                                                                                         |
| `DevicesTab.tsx:138`      | Device icon                                     | keep with reason | Uses `HugeiconsIcon` and marks the decorative laptop icon `aria-hidden`; the 16px icon size matches the existing compact Mobile Remote row treatment.                                                                                    | None.                                                                                                                                                                                                         |
| `DevicesTab.tsx:155`      | Desktop presence                                | keep with reason | Reuses `StatusDot` and semantic theme classes (`success`, `warning`, and text tokens), with a visible localized status label; no literal color escapes the theme.                                                                        | None.                                                                                                                                                                                                         |
| `MobileAuthGate.tsx:269`  | Authentication-state presentation               | keep with reason | The gate owns lifecycle/state but delegates every non-signed-in visible state to `MobileAuthScreen`, so the native runtime does not fork authentication UI.                                                                              | None.                                                                                                                                                                                                         |
| `MobileAuthScreen.tsx:36` | Authentication shell, loading, and error states | keep with reason | Reuses `MobileShell`, `Placeholder`, and `InlineAlert`; loading uses a polite live region and failures use `role=alert`, covering the visible accessibility states without custom modal or spinner scaffolding.                          | None.                                                                                                                                                                                                         |
| `MobileAuthScreen.tsx:78` | Sign-in action                                  | keep with reason | Uses the shared primary `Button` with a shared icon, full-width layout, native button semantics, and a centered label rather than raw interactive HTML.                                                                                  | None.                                                                                                                                                                                                         |

Verdict totals: **2 fix**, **8 keep with reason**, **0 abstract**.
