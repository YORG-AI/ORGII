# Chat message simplification UI audit

| Line                                                           | Element                        | Verdict          | Reason                                                                                                                                                                           | Suggested change                                                                                             |
| -------------------------------------------------------------- | ------------------------------ | ---------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------ |
| `src/engines/ChatPanel/ChatItems/AgentChatItemDefault.tsx:82`  | Assistant-message layout shell | keep with reason | This is message-specific flex layout with no interactive behavior or matching design-system container; removing the dead named hover group leaves only necessary layout classes. | None.                                                                                                        |
| `src/engines/ChatPanel/ChatItems/AgentChatItemDefault.tsx:122` | Result pill geometry           | fix candidate    | The arbitrary full-pill and 24px geometry has canonical utility equivalents and recurs across several components, so changing only this site would fragment the pattern.         | Handle as a separate compact-pill utility sweep (`rounded-full`, `h-6`, `py-0.5`) across all matching sites. |
| `src/engines/ChatPanel/ChatItems/AgentChatItemDefault.tsx:132` | Result action                  | keep with reason | The interactive action already uses the shared `Button` component with its secondary variant.                                                                                    | None.                                                                                                        |
| `src/engines/ChatPanel/blocks/AgentMessageBlock/index.tsx:174` | Long-message expand control    | keep with reason | `ExpandOverlay` is the established chat primitive for clamped content and owns the accessible expand/collapse interaction.                                                       | None.                                                                                                        |
| `src/engines/ChatPanel/blocks/AgentMessageBlock/index.tsx:193` | Simulator locate control       | keep with reason | `EventNavigateIcon` is the existing domain primitive and its `footer-hover` variant deliberately consumes the message wrapper's named hover group.                               | None.                                                                                                        |

Verdict totals: **1 fix candidate**, **4 keep with reason**, **0 abstract**.
