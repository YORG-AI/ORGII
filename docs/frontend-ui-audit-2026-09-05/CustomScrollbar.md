# CustomScrollbar UI audit

| Line                                           | Element                                  | Verdict          | Reason                                                                                                                                                  | Suggested change                                                                                                          |
| ---------------------------------------------- | ---------------------------------------- | ---------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| `src/components/CustomScrollbar/index.tsx:213` | Custom absolute scrollbar track          | keep with reason | The component is itself the reusable primitive and needs direct DOM geometry plus an overlay hit area that the general Button component cannot express. | None.                                                                                                                     |
| `src/components/CustomScrollbar/index.tsx:225` | Clickable marker `div`                   | fix candidate    | Optional clickable markers have pointer behavior but no keyboard semantics when `onClick` is provided.                                                  | In a separate accessibility change, add button semantics, focus handling, and an accessible label for actionable markers. |
| `src/components/CustomScrollbar/index.tsx:229` | Marker fallback `rgba(255, 165, 0, 0.9)` | fix candidate    | The literal warning color does not follow theme tokens.                                                                                                 | Replace the fallback with a semantic warning token after confirming marker contrast in both themes.                       |
| `src/components/CustomScrollbar/index.tsx:245` | Draggable thumb `div`                    | fix candidate    | The draggable thumb has no `role="scrollbar"`, value metadata, or keyboard scrolling behavior.                                                          | Add scrollbar semantics and keyboard controls as a dedicated accessibility follow-up.                                     |
| `src/components/CustomScrollbar/index.scss:7`  | Absolute 12px track with 6px inset thumb | keep with reason | The shared hit-area, thumb-size, and edge-inset tokens implement the requested overlay geometry without affecting the scroll container's layout.        | None.                                                                                                                     |
| `src/components/CustomScrollbar/index.scss:33` | 30px minimum thumb height                | keep with reason | A minimum drag target is required for long documents and is independent of the shared cross-axis width.                                                 | None.                                                                                                                     |

Verdict totals: **3 fix candidates**, **3 keep with reason**, **0 abstract**.
