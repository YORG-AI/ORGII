import { externalReplayTurnLoader } from "./externalReplayTurnLoader";

/** @deprecated Cursor uses the shared bounded-replay turn loader. */
export const cursorIdeTurnLoader = externalReplayTurnLoader;
